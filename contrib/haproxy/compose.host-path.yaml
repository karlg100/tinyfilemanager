services:
  tinyfilemanager:
    volumes:
      - type: bind
        source: "${TFM_DATA_PATH:?Set TFM_DATA_PATH to a dedicated absolute host directory}"
        target: /srv/tinyfilemanager/data
        bind:
          create_host_path: false
          selinux: Z
